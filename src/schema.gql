# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Product {
  product_id: ID!
  name: String!
  category: Category!
  category_id: ID!
}

type Category {
  category_id: ID!
  name: String!
  store: Store!
  store_id: ID!
  products: [Product!]!
}

type Store {
  store_id: ID!
  name: String!
  address: String
  logo: String
  user: User!
  user_id: ID!
  categories: [Category!]!
}

type User {
  user_id: ID!
  email: String!
  first_name: String!
  last_name: String!
  password: String!
  stores: [Store!]!
}

type Query {
  users: [User!]!
  user(user_id: String!): User!
  stores: [Store!]!
  categories: [Category!]!
  products: [Product!]!
  product(product_id: String!): Product!
}

type Mutation {
  createUser(first_name: String!, last_name: String!, password: String!, email: String!): User!
  createStore(name: String!, address: String, logo: String, user_id: ID!): Store!
  createCategory(name: String!, store_id: ID!): Category!
  createProduct(name: String!, category_id: ID!): Product!
}